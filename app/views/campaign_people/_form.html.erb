<%= form_for(@campaign_person) do |f| %>
  <% if @campaign_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign_person.errors.count, "error") %> prohibited this campaign_person from being saved:</h2>

      <ul>
      <% @campaign_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label :user_id %><br />
    <%= f.collection_select :user_id, User.all, :id, :username, :prompt => true, :required => true %>
  </div>
  <div class="field">
	<%= f.label :campaign_id %><br />
    <%= f.collection_select :campaign_id, Campaign.all, :id, :name, :prompt => true, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
