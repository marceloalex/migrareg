<%= form_for(@auxiliar_management) do |f| %>
  <% if @auxiliar_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auxiliar_management.errors.count, "error") %> prohibited this auxiliar_management from being saved:</h2>

      <ul>
      <% @auxiliar_management.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label :customer_id %><br />
    <%= f.collection_select :customer_id, Customer.all, :id, :code_customer, :prompt => true, :required => true %>
  </div>
  <div class="field">
	<%= f.label :wallet_id %><br />
    <%= f.collection_select :wallet_id, Wallet.all, :id, :name, :prompt => true, :required => true %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
