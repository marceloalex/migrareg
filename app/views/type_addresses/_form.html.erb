<%= form_for(@type_address) do |f| %>
  <% if @type_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_address.errors.count, "error") %> prohibited this type_address from being saved:</h2>

      <ul>
      <% @type_address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
