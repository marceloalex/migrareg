<%= form_for(@type_phone) do |f| %>
  <% if @type_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_phone.errors.count, "error") %> prohibited this type_phone from being saved:</h2>

      <ul>
      <% @type_phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
