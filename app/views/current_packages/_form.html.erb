<%= form_for(@current_package) do |f| %>
  <% if @current_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_package.errors.count, "error") %> prohibited this current_package from being saved:</h2>

      <ul>
      <% @current_package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :current_line %><br />
    <%= f.text_field :current_line %>
  </div>
  <div class="field">
    <%= f.label :current_speedy %><br />
    <%= f.text_field :current_speedy %>
  </div>
  <div class="field">
    <%= f.label :current_wire %><br />
    <%= f.text_field :current_wire %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
