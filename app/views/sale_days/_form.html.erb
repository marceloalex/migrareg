<%= form_for(@sale_day) do |f| %>
  <% if @sale_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_day.errors.count, "error") %> prohibited this sale_day from being saved:</h2>

      <ul>
      <% @sale_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label :wallet_id %><br />
    <%= f.collection_select :wallet_id, Wallet.all, :id, :name, :prompt => true, :required => true %>
  </div>
  <div class="field">
	<%= f.label :customer_id %><br />
    <%= f.collection_select :customer_id, Customer.all, :id, :code_customer, :prompt => true, :required => true %>
  </div>
  <div class="field">
    <%= f.label :hour %><br />
    <%= f.datetime_select :hour %>
  </div>
  <div class="field">
	<%= f.label :user_id %><br />
    <%= f.collection_select :user_id, User.all, :id, :username, :prompt => true, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
