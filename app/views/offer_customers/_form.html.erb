<%= form_for(@offer_customer) do |f| %>
  <% if @offer_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer_customer.errors.count, "error") %> prohibited this offer_customer from being saved:</h2>

      <ul>
      <% @offer_customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer %><br />
    <%= f.text_field :customer %>
  </div>
  <div class="field">
    <%= f.label :wallet %><br />
    <%= f.text_field :wallet %>
  </div>
  <div class="field">
    <%= f.label :offer %><br />
    <%= f.text_field :offer %>
  </div>
  <div class="field">
    <%= f.label :promotion %><br />
    <%= f.text_field :promotion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
